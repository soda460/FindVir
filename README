POur faire l'analyse

J'ai utilisé quelques lignes de biopython pour recuperer tous les CDS annotes dans
mes fichiers genbank (244 souches) et les nommer correctement

ensuite j'ai concatene le tout sous le nom MS_strains.fasta

ensuite j'ai construit la db NCBI blast+ avec la commande


makeblastdb -in MS_strains.fasta -input_type fasta -dbtype prot -title MS_strains -out MS_strains

Pour les queries, j'ai recupere les intims du papier chinois et j'ai fait un cd-hit

cd-hit -i intimin.pep -o intimin90 
